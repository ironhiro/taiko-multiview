# Optional. Leave unset to call /api on the same origin, which is what the
# Vercel rewrite and the Vite dev proxy both provide.
# Set it only when the frontend must reach a backend on another origin
# (the desktop shell does this).
VITE_API_BASE=
