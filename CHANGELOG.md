# 변경 내역

## 2026-09-26 · 모바일 개선

폰에서 보기 편하고, 오래 켜 두어도 덜 무겁게. 데스크톱 화면은 채팅이 빠진 것 말고는 그대로다.

### 폰 화면
- 매장 번호, LIVE, 시청자 수, 음소거 버튼이 영상 **아래 줄**로 내려갔다. 유튜브 자체 컨트롤(진행 막대, 채널 링크)은 막아서 스크롤하다 잘못 눌리지 않는다. 소리는 음소거 버튼으로 켠다.
- 상단에는 매장 줄과 보기 줄만 고정되고, 송출 수·새로고침 줄은 스크롤과 함께 올라간다.
- 매장 탭이 한 줄에 다 들어가지 않으면 **선택한 매장 + "전체 매장 ▾"** 목록으로 접힌다. 목록 버튼에는 다른 매장의 송출 수가 붙는다. 매장이 늘어도 상단 바 높이는 그대로(세로 폰 108px).
- 가로로 든 아이폰(폭 844~932px)도 폰 화면으로 보인다. 타일 하나가 화면 높이를 채운다.

### 폰 재생 규칙
- **동시에 재생하는 영상은 2개까지.** 화면에 가장 많이 보이는 타일 두 개가 재생되고(상단 바에 가려진 부분은 안 보이는 것으로 친다), 멈춰 있는 타일을 탭하면 그 타일이 먼저 재생된다.
- 플레이어는 최대 4개만 남긴다. 화면 밖으로 나간 타일은 멈춰 두었다가 돌아오면 **라이브 시점부터** 이어서 재생한다.
- 앱을 내리거나 탭을 바꾸면 즉시 모두 멈추고, 3분이 지나면 플레이어를 정리한다. 돌아오면 보이는 타일부터 다시 재생한다.
- 메모리가 4GB 미만인 기기는 1개만 재생한다(안드로이드 크롬, HTTPS 접속에서 판별).
- 데이터 절약 모드나 느린 회선(3G 이하)에서는 자동재생하지 않고, 탭한 타일만 재생한다(안드로이드 크롬).

### 제거
- **채팅 기능을 뺐다**(웹과 데스크톱 앱 모두). 브라우저에서는 채팅창에 유튜브 로그인이 전달되지 않아 입력할 수 없었다.
- 데스크톱 앱은 여전히 멀티뷰 창이 다른 사이트로 넘어가지 않게 막고, 넘어가려던 페이지는 기본 브라우저로 연다.

### 안정성
- 첫 접속 때 매장 목록을 받지 못하면 자동으로 다시 시도한다(2초, 4초, 8초… 최대 30초 간격). 응답이 15초 넘게 없으면 끊고 다시 시도한다. 그동안 "서버에 다시 연결하는 중…"이 보인다. 전에는 오류 화면에 멈춰서 브라우저를 새로 고쳐야 했다.

### 성능
실제 매장(TAIKO LABS, 9기체), iPhone 15 Pro 에뮬레이션, 20초 스크롤, 3~4회 측정 평균.

| | 전 | 후 |
|---|---|---|
| 동시 재생 | 5개 | **2개** |
| 20초 동안 만든 플레이어 | 23~25개 | **4개** |
| CPU | 1.05코어 | **0.84코어** (−20%) |
| 메모리 최대 | 1,235MB | **802MB** (−35%) |

데스크톱 3×3은 9개 모두 재생하고, 가장 긴 화면 멈춤이 133ms에서 34ms로 줄었다.

### 알려진 한계
- 맨 위와 맨 아래를 계속 오가는 거친 스크롤(50초)에서는 플레이어를 새로 만드는 횟수가 70에서 100으로 늘었다. 플레이어를 4개까지만 남기는 대가다. 메모리 최대치는 비슷하고, 페이지 JS 메모리는 70MB에서 30MB로 줄었다.
- 에뮬레이터로 확인할 수 없어 실제 기기에서 볼 것: 아이폰 사파리에서 주소창이 접히고 펴질 때 재생, 1~2분 스크롤 뒤 탭이 죽지 않는지, 다른 앱에 다녀온 뒤 라이브 시점 복귀, 저전력 모드에서 탭 재생, 목록 바깥을 눌러 닫기.

### 개발
- 로컬 부하 테스트: 가짜 매장(`scripts/mock-venues.mjs`)과 실제로 재생되는 24시간 방송으로 `npm run perf`(플레이어 수, 재생 수, CPU, 메모리, 다운로드, 거친 스크롤 시나리오). `frontend/perf/README.md` 참고.
- 폰 화면·상단 바·첫 로드 재시도 e2e 테스트.
- Claude Code 하네스: 변경을 구현부터 기기별 확인·성능 비교까지 처리하는 개발 하네스와, 아키텍처·보안·성능·스타일을 함께 보는 코드 리뷰 하네스(`.claude/`, `CLAUDE.md`).

## 2026-09-26 · 보안

- 보안 헤더(CSP, HSTS, nosniff, X-Frame-Options, Referrer-Policy)를 붙이고 `Server` 헤더를 뺐다.
- 새로고침 요청을 IP당 분당 6회로 제한했다(넘으면 429, 화면은 일반 조회로 대신한다).
- 취약점 제보 절차(`SECURITY.md`)를 추가했다. 이슈 #1 대응.
