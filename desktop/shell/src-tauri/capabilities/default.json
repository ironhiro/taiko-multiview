{
  "identifier": "default",
  "description": "The multiview window calls no app command of its own; it keeps Tauri's core defaults only.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
